# macOS
.DS_Store

# Reasonix / agent metadata — never publish
.reasonix/
reasonix.toml

# Temporary downloads / scratch
*.tmp
downloads/

# Release bundles built by scripts/make-release.sh
dist/

# Deployed console root (built by the installers) — keep ROMs out of git
retro-home/
