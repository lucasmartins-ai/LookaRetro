# LookaRetro — RetroArch platform driver snippet (macOS / Apple Silicon)
# Appended to the shared config by install-macos.sh.
video_driver = "metal"
audio_driver = "coreaudio"
video_windowed_fullscreen = "true"
