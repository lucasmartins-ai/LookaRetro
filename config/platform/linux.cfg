# LookaRetro — RetroArch platform driver snippet (Linux)
# Appended to the shared config by install-linux.sh.
# For better latency on modern GPUs, try: video_driver = "vulkan"
video_driver = "gl"
audio_driver = "alsa"
