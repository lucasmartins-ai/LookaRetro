# LookaRetro — per-core override: Mupen64Plus-Next (Nintendo 64)
# ParaLLEl RDP (GPU) plugin is the fast, accurate default on Apple Silicon.
# run-ahead is NOT supported here; low latency comes from hard GPU sync.
video_scale_integer = "false"
video_smooth = "true"
