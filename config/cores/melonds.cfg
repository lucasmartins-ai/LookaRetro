# LookaRetro — per-core override: melonDS (Nintendo DS)
# melonDS does not support run-ahead. Keep global hard-sync/low-latency
# settings and use integer scaling for the two-screen layout.
video_scale_integer = "false"
video_smooth = "true"
