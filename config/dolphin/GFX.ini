# LookaRetro — minimal Dolphin GFX config (Wii / GameCube)
# Only the fullscreen flag is set here; Dolphin fills in the rest of its
# defaults on first launch and preserves this setting.
[Settings]
Fullscreen = True
